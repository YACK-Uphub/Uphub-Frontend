// @flow 
import * as React from 'react';

export type UModalProps = {
	isOpen: boolean;
	onCloseModal: () => void;
	children: React.ReactNode;
};
export const UModal = ({isOpen, onCloseModal, children}:UModalProps) => {
	return (
		<div>

		</div>
	);
};